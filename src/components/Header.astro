---
import LanguagePicker from './LanguagePicker.astro'
import * as m from '../paraglide/messages'
---
<header class="p-6 v-center">
    <a
      href="/"
      class="font-black text-white"
    >CorpusHub</a>
    <nav>
        <ul class="flex gap-4">
          {[{
              path: '/concordancers',
              name: 'Concordancers'
            },{
              path: '/corpora',
              name: 'Corpora'
            },{
              path: '/datasets',
              name: 'Datasets'
            },{
              path: '/dictionaries',
              name: 'Dictionaries'
            }
          ].map(({path, name}) => (
            <li><a href={path}
              class={path === Astro.url.pathname ? 'active' : ''}
            >{name}</a></li>
          ))}
        </ul>
    </nav>
    <span
      class="flex-grow"
    ></span>
    <span>
      {m.login()}
    </span>
    <LanguagePicker />
</header>

<style>
    header {
        color: white;
        display: flex;
        gap: 2em
    }

    nav ul {
        list-style-type: none;
        margin: 0;
        padding: 0;
    }

    nav ul li {
        display: inline;
    }

    nav ul li a {
        color: white;
        text-decoration: none;
    }

    nav ul li a:hover {
        text-decoration: underline;
    }

    nav ul li a.active {
        font-weight: bold;
    }
</style>